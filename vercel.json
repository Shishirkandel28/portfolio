{
  "_comment": "Vercel configuration for Shishir Kandel's Portfolio Website",
  "_description": "This config builds the React portfolio and serves it as a static site with SPA routing",
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "_comment": "Handle SPA routing - all routes serve index.html for client-side navigation",
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "functions": {
    "_comment": "Configure any serverless functions if needed in the future"
  }
}